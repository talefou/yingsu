name: Generate Reports Branch

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 0 */2 * *'  # 每两天执行一次

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: Run build script
      run: npm start

    - name: Create or update reports branch
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        
        git checkout -B reports
        
        # 清空当前分支所有文件
        git rm -rf .
        
        # 添加整个 reports 文件夹
        git add reports
        
        # 复制 reports 中的 HTML/JSON 文件到根目录，并重命名
        cp reports/speed-test-report-*.html ./index.html
        cp reports/speed-test-report-*.json ./db.json
        
        # 添加到 Git
        git add index.html db.json
        
        # 提交变更
        git commit -m "Update reports from build $(date)"
        
        # 强制推送到 reports 分支
        git push -f origin reports
